---
interface Props {
  imageSrc: string;
  imageAlt: string;
  title: string;
  text: string;
  imageLeft?: boolean;
}

const {
  imageSrc,
  imageAlt,
  title,
  text,
  imageLeft = true,
} = Astro.props;
---

<section id="aboutMe" class={`feature ${imageLeft ? "" : "reverse"}`}>
  <div class="feature-image">
    <img src={imageSrc} alt={imageAlt} />
  </div>

  <div class="card feature-content">
    <h2>{title}</h2>
    <div class="about-text" set:html={text}></div>
  </div>
</section>

<style>
  .feature {
  display: flex;
  align-items: center;
  gap: 4rem;
  padding: 4rem;
}

.feature.reverse {
  flex-direction: row-reverse;
}

.feature-image {
  flex: 0 0 350px; /* image column width */
}

.feature-content {
  flex: 1; /* use remaining space */
}

.feature-image img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 12px;
}

.card {
    background: #1f1f1f;
    border-radius: 16px;
    padding: 0rem 2rem;
    box-shadow: 0 2px 8px rgba(0,0,0,.6);
}
</style>


